* Given
** A lex file
** A yacc file
** Some samplel snippets of program
* Target 
** execute the program statements
** make an AST and print it

* Procedure
  - (x) First ensure that the basic calculator stuff is working with lex and yacc file - like add, sub, mul, div etc. 
  - (-) Then, add functionality for multicharacter variables
  - (x) Then make Node and define it in union, change all $S to appropriate stuffs
  - (x) Now, take sentences and link them in a linked list

* ToDo
** Debug code to make it evaluatable. 
** print the linked list. 
